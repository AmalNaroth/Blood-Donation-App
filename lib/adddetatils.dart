import 'package:flutter/material.dart';

class AddDetatils extends StatelessWidget {
  const AddDetatils({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}